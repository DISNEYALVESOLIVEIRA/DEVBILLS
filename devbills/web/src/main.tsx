import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App.tsx'

createroot(documente.getelementById('root')!).render(
    <StrictMode>
        <App />
    </StrictMode>
)